{
  "name": "fantasy-football",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "deploy": "cd frontend && npm run build && cd ../server && npm run build && npm start",
    "dev": "npm run dev --prefix frontend & npm start --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
